import { RainbowDirective } from './highlight-mouse.directive';

describe('HighlightMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new RainbowDirective();
    expect(directive).toBeTruthy();
  });
});
