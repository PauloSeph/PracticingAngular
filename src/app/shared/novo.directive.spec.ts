import { NovoDirective } from './novo.directive';

describe('NovoDirective', () => {
  it('should create an instance', () => {
    const directive = new NovoDirective();
    expect(directive).toBeTruthy();
  });
});
