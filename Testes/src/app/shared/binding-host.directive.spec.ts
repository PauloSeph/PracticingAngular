import { BindingHostDirective } from './binding-host.directive';

describe('BindingHostDirective', () => {
  it('should create an instance', () => {
    const directive = new BindingHostDirective();
    expect(directive).toBeTruthy();
  });
});
