import { OnlyMyBaconDirective } from './only-my-bacon.directive';

describe('OnlyMyBaconDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyMyBaconDirective();
    expect(directive).toBeTruthy();
  });
});
