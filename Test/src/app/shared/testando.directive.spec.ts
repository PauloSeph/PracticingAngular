import { TestandoDirective } from './testando.directive';

describe('TestandoDirective', () => {
  it('should create an instance', () => {
    const directive = new TestandoDirective();
    expect(directive).toBeTruthy();
  });
});
